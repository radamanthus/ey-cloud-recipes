/var/log/engineyard/nginx/*log {
    rotate 72
    compress
    size 1
    nodateext

    missingok
    notifempty
    sharedscripts
    extension gz
    copytruncate
      postrotate
          [ ! -f /var/run/nginx.pid ] || kill -USR1 `cat /var/run/nginx.pid`

      endscript
}
