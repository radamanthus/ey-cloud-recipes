 #!/bin/bash

case $1 in
  start)
  /usr/local/bin/diresource -b 127.0.0.1:<%= @port %> 2>&1 1>/var/log/diresource.log &
    ;;
  stop)
    pkill diresource ;;
    *)
  echo "usage: /etc/init.d/diresource {start|stop}" ;;
esac
exit 0
